# 组协作目录实现计划

> **效力：** Grill 两轮决策已锁定（§0）。实现时以本节为准；不替代 [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md) 的验证版基线。
> **目标：** 每人保留私有目录 + 可属 **多个** 组协作目录；每人仍独享 `runner-{user}` 容器与端口。

---

## 0. 决策确认（Plan 模式）

### 0.1 Round 1 — 已锁定 ✅

| ID | 决策 | 结论 |
|----|------|------|
| Q1 | 一人多组 | ✅ 支持多组；login 返回 `groups[]`；容器按组多路 bind |
| Q2 | NFS 组目录命名 | ✅ `/mnt/dockerContainer/nfs/groups/{group_id}` |
| Q3 | 配置唯一源 | ✅ `serverA/config/groups.yaml` 为唯一源 |
| Q4 | 协作区挂载权限 | ✅ 读写 `rw` |
| Q5 | 组管理 | ✅ 仅运维改 YAML + `nfsctl apply`；无管理 API |
| Q6 | 试点范围 | ✅ `team-alpha`：alice、bob；`team-beta`：carol、dave |

### 0.2 Round 2 — 已锁定 ✅

| ID | 决策 | 结论 |
|----|------|------|
| Q7 | 容器内多组挂载 | ✅ `/workspace/groups/{group_id}`（与 NFS `groups/` 一致） |
| Q8 | login API | ✅ `groups: []` 数组；无组时 `[]` |
| Q9 | nfsserver 对齐 | ✅ `nfsctl sync-groups` 读 `serverA/groups.yaml` 生成 nfsserver 配置 |
| Q10 | 协作脚本路径 | ✅ `groups/{group_id}/jobs/train.py`（相对 `/workspace`） |
| Q11 | 训练产物落盘 | ✅ 日志 / checkpoint / TB 仍只写私有 `/workspace`；协作区放代码与共享数据 |

### 0.3 决策树（已全部闭合）

```
Q1 多组 ──► Q7 /workspace/groups/{id} ──► Q10 groups/{id}/jobs/...
Q2 groups/{id} ──► NFS 导出路径
Q3 ServerA 源 ──► Q9 nfsctl sync-groups
Q6 两组试点 ──► P-G1 验收
Q11 产物私有盘 ──► CLIENT.md 约定
```

---

## 1. 要解决的问题

| 现状 | 目标 |
|------|------|
| 每人只有私有 NFS 目录 | 保留私有目录，**额外** 挂 0～N 个组协作目录 |
| alice 与 bob 私有文件隔离（I-04） | **不变**；仅 **同组协作路径** 互相可见 |
| 每人 `runner-{user}` 独立容器 | **不变** |

**一句话：** 文件分「私有 + 多组协作」，计算仍按人隔离。

---

## 2. 目标架构

```
客户端 (alice ∈ team-alpha, team-beta 若未来扩展)
  ├─ mount 私有：115:.../nfs/alice → /mnt/nfs/alice
  ├─ mount 协作（每组一条）：
  │     115:.../nfs/groups/team-alpha → /mnt/nfs/groups/team-alpha
  ├─ POST /login → token + nfs_* + groups[]
  └─ POST /containers/start → 每人独立 server_b / obs / tensorboard

Server A
  ├─ 本机挂载私有 + 该用户所属全部组目录
  └─ docker run runner-alice
        -v /mnt/nfs/alice:/workspace
        -v /mnt/nfs/groups/team-alpha:/workspace/groups/team-alpha
        -p 10.213.35.42:31xxx:8080
        ...

NFS Server (115)
  ├─ /mnt/dockerContainer/nfs/alice
  ├─ /mnt/dockerContainer/nfs/bob
  ├─ /mnt/dockerContainer/nfs/groups/team-alpha    ← 试点
  └─ /mnt/dockerContainer/nfs/groups/team-beta     ← 试点
```

**约束：**

- 私有工作区：`/workspace`（不变）。
- 第 i 个协作区：`/workspace/groups/{group_id}`（与 NFS `groups/{group_id}` 一一对应）。
- 无组用户：`groups: []`，容器 **不** 挂协作 volume。
- 有 n 个组：容器 **n 路** 协作 bind + 1 路私有 bind。
- 每人仍只允许一只 `runner-{user}`。

**非目标（v1）：**

- 组级容器 / 组级 Server B / 组级画面或 TB。
- 用户自助建组 API。
- K8s 多机组 PVC（见 [`K8S_PLAN.md`](K8S_PLAN.md)）。

---

## 3. 配置模型

### 3.1 唯一源 — `serverA/config/groups.yaml`

```yaml
groups:
  team-alpha:
    nfs_host: "10.250.30.115"
    nfs_export_path: "/mnt/dockerContainer/nfs/groups/team-alpha"
    local_mount_path: "/mnt/nfs/groups/team-alpha"
    members: [alice, bob]
  team-beta:
    nfs_host: "10.250.30.115"
    nfs_export_path: "/mnt/dockerContainer/nfs/groups/team-beta"
    local_mount_path: "/mnt/nfs/groups/team-beta"
    members: [carol, dave]
```

**约束：**

- `members` 中的用户名必须在 `users.yaml` 存在。
- 同一用户可出现在多个 `members` 列表（Q1）。
- 加载时反查：`UserRecord.groups: list[str]`（组 id 列表）。

### 3.2 nfsserver — 由 Server A 同步生成

**不手改** `nfsserver/config/users.yaml` 里的 `groups` 段；运维流程：

```bash
cd nfsserver
python nfsctl.py sync-groups    # 读 ../serverA/config/groups.yaml，写 groups 导出段
python nfsctl.py apply          # SSH 115：mkdir + exportfs
```

`nfsserver/config/users.yaml` 保留 **个人** `users`；`groups` 段带 `# generated by sync-groups` 标记。

### 3.3 `serverA/config/server.yaml` 新增

```yaml
server:
  container_workspace: "/workspace"
  container_groups_prefix: "/workspace/groups"   # 挂载点 = prefix + "/" + group_id
  nfs_mount_root: "/mnt/nfs"
```

---

## 4. API 变更

### 4.1 `POST /login` — `LoginResponse`

**新增：**

```json
"groups": [
  {
    "group_id": "team-alpha",
    "nfs_host": "10.250.30.115",
    "nfs_export_path": "/mnt/dockerContainer/nfs/groups/team-alpha",
    "local_mount_hint": "/mnt/nfs/groups/team-alpha"
  }
]
```

**无组用户：** `"groups": []`。

**不再使用** 单字段 `group_id` / `group_nfs_*`（v1 新客户端以数组为准；若需兼容可另开 Round，当前 **不保留** 扁平字段）。

其余字段（`token`、`nfs_host`、`nfs_export_path`、`server_b_endpoint`…）不变。

### 4.2 `POST /containers/start` / `GET /containers/current`

**新增：**

```json
"group_mounts": [
  {"group_id": "team-alpha", "container_path": "/workspace/groups/team-alpha"}
]
```

无组：`[]`。`nfs_mount_path` 仍为 `/workspace`。

### 4.3 Server B — 无 API 变更

| 脚本位置 | `script_path` |
|----------|---------------|
| 私有 | `jobs/train.py` |
| 协作 | `groups/team-alpha/jobs/train.py` |

训练产物（日志、TB、checkpoint）**必须**写在私有盘路径下，例如 `logs/`、`jobs/last_run.txt`。

---

## 5. 实施阶段（可勾选）

### P-G1 — NFS 组导出（115 轨道）

- [ ] `serverA/config/groups.yaml` 写入试点两组
- [ ] `nfsctl.py` 实现 `sync-groups`
- [ ] `nfsctl.py apply` 在 115 创建 `groups/team-alpha`、`groups/team-beta`
- [ ] `showmount -e` 可见两条组导出
- [ ] alice 笔记本双挂载私有 + `groups/team-alpha`，与 bob 互写探针文件
- [ ] carol / dave 对 `team-beta` 同样验证
- [ ] **team-alpha 与 team-beta 互不可见**（跨组隔离探针）

**不依赖 Server A 代码变更。**

---

### P-G2 — Server A 多路挂载 + API

- [ ] `app/config.py` 加载 `groups.yaml`，填充 `UserRecord.groups`
- [ ] `app/schemas.py`：`GroupMountInfo`、`groups[]`、`group_mounts[]`
- [ ] `app/routers/auth.py`：login 填充 `groups`
- [ ] `app/nfs.py`：`ensure_user_groups_nfs(user)` 挂载用户全部组目录
- [ ] `app/docker_mgr.py`：`volumes` 支持动态多组 bind
- [ ] `app/container_service.py`：start 前 ensure 私有 + 全部组 NFS

**单元测试：**

- [ ] T-G-01 alice login `groups` 含 `team-alpha`
- [ ] T-G-02 carol login `groups` 含 `team-beta`；eve login `groups` 为 `[]`
- [ ] T-G-03 alice（单组）start 后两路 Mounts：私有 + `groups/team-alpha`
- [ ] T-G-04 未来 alice 属两组时三路 Mounts（可先 mock 配置测 docker_mgr）
- [ ] T-G-05 bob 容器可见 `groups/team-alpha/probe.txt`
- [ ] T-G-06 alice 容器 **不可见** bob 私有文件（I-04）
- [ ] T-G-07 carol 容器 **不可见** `groups/team-alpha` 下文件（跨组隔离）

**联调：**

- [ ] I-G-01 alice、bob 同组各 start，各跑 `groups/team-alpha/jobs/hello.py`
- [ ] I-G-02 carol、dave 同组各 start，各跑 `groups/team-beta/jobs/hello.py`
- [ ] I-G-03 team-alpha 与 team-beta 交叉 `docker exec` 不可见对方组目录文件

---

### P-G3 — 客户端文档

- [ ] 更新 [`docs/CLIENT.md`](docs/CLIENT.md)：login `groups[]`、多 mount、`group_mounts[]`
- [ ] 路径约定：`groups/{group_id}/jobs/...` vs 私有 `jobs/...`
- [ ] 排障：跨组不可见、协作区勿写 TB 事件
- [ ] （可选）端到端 smoke：`tests/units/端到端/组协作登录到开训/`

---

## 6. NFS 权限

**v1（验证环境）：** 组目录 `chmod 0777`（与现有用户目录一致）。

**文档必须写明：**

- 协作区多人同时写同一文件可能互相覆盖。
- checkpoint / TB 请放私有 `/workspace`，不要放 `groups/` 下。

**后续（非 v1）：** 固定 gid + `2775` 或 UID 映射。

---

## 9. 与现有测试的关系

| 现有用例 | 要求 |
|----------|------|
| I-04 bob 隔离 | **必须保持**（私有目录） |
| T-NFS-03 | 不变 |
| T-D-03 | 不变；新增 T-G-* / I-G-* |

---

## 10. 实施顺序

```
P-G1  groups.yaml + sync-groups + 115 导出 + 手工多挂载验收
  ↓
P-G2  serverA 配置 / nfs / docker_mgr / API / T-G-* 
  ↓
P-G2  联调 I-G-01～03
  ↓
P-G3  CLIENT.md
```

**预估：** P-G1 半天 · P-G2 1.5–2 天 · P-G3 半天

---

## 11. 参考路径

| 角色 | 路径 |
|------|------|
| NFS 主机 | `10.250.30.115` |
| 组导出（示例） | `/mnt/dockerContainer/nfs/groups/team-alpha` |
| Server A 本机组挂载 | `/mnt/nfs/groups/team-alpha` |
| 容器内协作挂载 | `/workspace/groups/team-alpha` |
| 客户端建议挂载 | `/mnt/nfs/groups/team-alpha` |

---

## 12. Definition of Done

- [x] Round 1 + Round 2 决策已全部写入 §0 且与实现一致
- [x] 115 上两组导出可用；组成员笔记本可多挂载读写
- [x] 跨组协作目录互不可见
- [x] login 返回 `groups[]`；start 返回 `group_mounts[]`
- [x] 有组用户容器内存在 `/workspace/groups/{id}` 且同组成员可见相同文件
- [x] 私有目录隔离（I-04）仍成立
- [x] 每人容器、端口、画面、TB 仍独立
- [x] `docs/CLIENT.md` 已更新
- [x] `tests/units/` 下 T-G-01～07 绿灯
